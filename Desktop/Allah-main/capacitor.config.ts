import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.copytrade.platform',
  appName: 'copytrade-platform',
  webDir: 'dist'
};

export default config;
